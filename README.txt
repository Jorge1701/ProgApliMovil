###########################################################################################
									PARA EJECUTAR
*Copiar el archivo WebMovil.war en la carpeta webapps del tomcat
*Copiar el archivo configuracion.properties en la carpeta properties del tomcat
*Ejectuar el script startup.bat dentro de la carpeta bin del tomcat